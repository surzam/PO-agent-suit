import assert from 'node:assert/strict';
import fs from 'node:fs/promises';
import {projectSurfaces,deriveSurfaceActions} from '../interop/ag-ui/surfaces.mjs';
const session={session:{runId:'run-actions-1',status:'completed',lastSequence:20},role:{id:'product-owner'},activity:{items:[]},workspace:{},research:{sources:[{sourceId:'source-1',state:'read'}],evidence:[{id:'evidence-1',sourceId:'source-1',claim:'Fact'}],validation:[{evidenceId:'evidence-1',valid:true}]},outputs:{artifacts:[{artifactId:'story-1',type:'Narrative'}]},ui:{pendingInteraction:null}};
const first=projectSurfaces(session),second=projectSurfaces(structuredClone(session)),evidence=first.items.find(item=>item.kind==='evidence'),source=first.items.find(item=>item.kind==='source'),story=first.items.find(item=>item.kind==='story');assert.deepEqual(first,second);assert.deepEqual(evidence.actions.map(item=>item.id),['action:research.extend:evidence:evidence-1']);assert.equal(evidence.actions[0].inputBinding.evidenceRef,'evidence-1');assert.equal(source.actions[0].inputBinding.sourceRef,'source-1');assert.equal(story.actions.length,0);assert.equal(deriveSurfaceActions({surface:evidence,session,capabilities:[]}).length,0);assert.equal(deriveSurfaceActions({surface:evidence,session:{...session,session:{...session.session,status:'running'}}}).length,0);const cto=projectSurfaces({...session,role:{id:'cto'}}),ctoAction=cto.items.find(item=>item.kind==='evidence').actions[0];assert.equal(ctoAction.capabilityId,evidence.actions[0].capabilityId);assert.notEqual(ctoAction.label,evidence.actions[0].label);assert.equal(new Set(first.items.flatMap(item=>item.actions.map(action=>action.id))).size,first.items.flatMap(item=>item.actions).length);
const renderer=await fs.readFile(new URL('../public/ui/observation/observation-mode.js',import.meta.url),'utf8');assert.match(renderer,/surface\?\.actions|surface\.actions|item\.actions/);assert.match(renderer,/data-surface-action/);assert.doesNotMatch(renderer,/research\.extend[^\n]*<button/i);assert.match(renderer,/INVOKE_CAPABILITY|invokeCapability/);console.log('action surfaces audit: contextual derivation · stable identity · role labels · no invented Story action · generic renderer · PASS');

import assert from 'node:assert/strict';import {decisionDeckFromSemantic} from '../core/decision-deck.mjs';const deck=decisionDeckFromSemantic({narrative:{id:'n',supportingClaims:['c']},hypothesis:{id:'h',primaryMetric:{id:'m'}},charts:[{id:'ch'}]});assert.deepEqual(deck.map(s=>s.role),['decision','facts','data','narrative','hypothesis','validation-plan','proposal']);assert.equal(deck[1].refs[0],'c');console.log('PO_DECISION_DECK_AUDIT PASS');
